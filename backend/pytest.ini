[pytest]
testpaths = app/tests
pythonpath = .
addopts = -ra -q --durations=10
markers =
    unit: fast tests, no external services
    integration: tests that require docker services (neo4j/postgres/qdrant)