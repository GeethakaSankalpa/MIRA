# Milestone M0 — Repo + Infra Setup

## Goal
Initialize repository and run local infrastructure (Neo4j, Postgres, optional Qdrant).

## What I Built
- Git repository initialized
- Folder structure created
- Docker Compose services running
- .env configuration created

## Commands Used
- git init
- docker compose up -d (access to E:\Mira\infra)

## What to consider 
- NEO4J_USER / neo4j admin must be neo4j in .env file (neo4j enforced rule)

## What Works Now
- Neo4j UI accessible on http://localhost:7474
- Postgres running on localhost:5432
- Qdrant running on http://localhost:6333

## Notes / Issues
- (Write any issues you faced and how you fixed them)
