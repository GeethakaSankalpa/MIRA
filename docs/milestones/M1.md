# Milestone M1 — Core API + Config + Logging + Tests

## Goal
Create a working FastAPI backend with configuration management, logging, and a health endpoint.

## What I Built
- FastAPI app scaffold (`backend/app/main.py`)
- Config system using `.env` (`backend/app/core/config.py`)
- Logging setup (`backend/app/core/logging.py`)
- `/health` endpoint
- Pytest baseline test

## How to Run
```powershell
cd backend
.\.venv\Scripts\activate
uvicorn app.main:app --reload --port 8000

## How to Test
cd backend
pytest -q
Checkpoint

/health returns ok

tests pass